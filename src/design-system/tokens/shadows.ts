/**
 * Elevation/shadow tokens, defined in `./theme.css`. Prefer Tailwind utilities
 * (`shadow-md`, `shadow-editorial`) in components.
 */

export const shadows = {
  sm: "var(--shadow-sm)",
  md: "var(--shadow-md)",
  lg: "var(--shadow-lg)",
  editorial: "var(--shadow-editorial)",
} as const;
