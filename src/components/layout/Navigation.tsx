import Link from "next/link";

/** Placeholder — primary site navigation. No visual design yet. */
export function Navigation() {
  return (
    <nav>
      <Link href="/">Springboard</Link>
    </nav>
  );
}
