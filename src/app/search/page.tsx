import { Search } from "@/components/search/Search";

/** Placeholder — query wiring against content.search_vector comes with the CMS phase. */
export default function SearchPage() {
  return (
    <div>
      <h1>Search</h1>
      <Search />
    </div>
  );
}
